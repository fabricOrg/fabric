{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["docs/api/openapi.internal.json"],
  "//": "Turbo caches package tasks; global pass-through variables belong to root scripts and are not cache inputs. globalDependencies carries docs/api/openapi.internal.json because the api build copies it into dist/ — the file lives outside services/api, so nothing else makes turbo re-run that build when the spec changes, and a cache hit would restore a dist holding the previous document.",
  "globalPassThroughEnv": [
    "DATABASE_URL_APP",
    "DATABASE_URL_SUPER",
    "DASHBOARD_API_KEY",
    "DEV_TENANT_ID",
    "GITHUB_BASE_REF",
    "GITHUB_HEAD_REF",
    "GITHUB_OUTPUT",
    "GITHUB_REF_NAME",
    "LIVE_API_KEY",
    "LIVE_SENDER_COUNTRY",
    "LIVE_SENDER_ID",
    "LIVE_TENANT_ID",
    "SEED_OWNER_EMAIL",
    "SEED_STAFF_EMAILS",
    "SEED_WALLET_MINOR",
    "VERIFY_BASE_SHA",
    "WORKOS_ORGANIZATION_ID"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
