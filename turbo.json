{
  "$schema": "https://turbo.build/schema.json",
  "//": "Turbo caches & parallelizes tasks across packages. `typecheck`/`test` run per-package; `build` waits for upstream package builds (^build) so dependents compile against fresh types.",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
