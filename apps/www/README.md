# Fabric www

`apps/www` is Fabric's public static Astro + Starlight site for the marketing landing page and developer docs. Run it locally with `pnpm --filter @app/www dev`; it builds to static files with `pnpm --filter @app/www build` and is intended for manual `vercel deploy`.
