# SDK-005 send and observe managed SMS in sandbox — evidence

> Status: **scope COMPLETE, all 8 ACs closed — updated 2026-07-19.** Every SDK-005 scope item has
> real-Postgres evidence; the sandbox capability operates end to end (persistence, wallet path,
> worker, webhooks, logs, SDK, dashboard, recovery, retention, DSR). Live provider route stays OFF
> per the release gate. ADR-0005 remains `proposed` (product+security sign-off required for rollout).
>
> 2026-07-19 amendment: AC05 and AC07 were still recorded as partial after the work closing them had
> landed. Closing AC05 properly required writing the managed-specific negative tests, which
> **uncovered a live defect** — insufficient funds returned an opaque 500 instead of the declared
> 402 `insufficient_funds_error`. Fixed and regression-guarded.

## Outcome so far

A developer sends a released definition by stable key: `POST /v1/message-deliveries` (SDK
`fabric.messages.send`) renders through the same engine as preview, reserves sandbox funds, and
atomically persists one **delivery + attempt + message + acceptance outbox event** under a required
`Idempotency-Key`. The delivery id is **deterministic** per tenant/application/environment/key, so a
replay — including a concurrent race — returns the same resource with zero duplicate effects. The
shared transition core reconciles the message outcome back onto the attempt and delivery
(status, exact cost, `resource_version` bump), and `GET /v1/message-deliveries/:id`
(SDK `messages.retrieveDelivery`) reads it back with attempt history.

## Acceptance traceability

| Criterion | Evidence |
| --- | --- |
| SDK-005-AC01 (202 only after one durable funded delivery + recoverable attempt) | `managed-messages.integration` "persists one delivery + attempt + message": 202 body carries the reconciled delivery; row counts messages=1, deliveries=1, attempts=1; wallet reserve rides the same `prepareSend` transaction (insufficient funds fails the request synchronously). |
| SDK-005-AC02 (concurrent identical requests → one delivery/message/reservation/event; every caller gets it) | "collapses concurrent identical sends onto one delivery": 3 parallel sends, all 202, one delivery id, one attempt row — `FOR UPDATE` replay probe + `ON CONFLICT (tenant, app, env, idempotency_key) DO NOTHING`. |
| SDK-005-AC03 (same key + changed input → `idempotency_conflict`, no second effect, no 24h-cache dependence) | "409s when the same Idempotency-Key carries a different request": sha256 canonical request fingerprint compared in-row (durable, not cache-based); counts unchanged. |
| SDK-005-AC04 (crash recovery without duplicate provider contact/ledger movement) | `managed-crash-recovery.integration`: tx1 committed (message + reserve + delivery + attempt + acceptance event), no dispatch (= the crash); the standing sweeper converges message/delivery/attempt on `expired`, bumps `resource_version`, emits the terminal outbox event, refunds exactly once (second pass no-op), and a provider spy proves zero provider contact. |
| SDK-005-AC05 (pre-acceptance gates → structured error, no delivery) | **Closed 2026-07-19.** Cost ceiling: "fails closed on limits.max_cost before any write" (400 `max_cost_exceeded`, counts unchanged). Invalid payload: 400 path-coded `missing_required`. Managed-specific negatives now exist — `managed-negative.integration` proves a suppressed recipient (400 `recipient_opted_out`) and an underfunded wallet (**402 `insufficient_funds`**) each leave message/delivery/attempt/outbox counts unchanged, with a solvent control send (202) so the negatives can't pass for the wrong reason. **This test found a real defect:** the wallet's `InsufficientFundsError` was unmapped and escaped as an opaque **500**, so callers could not branch on it despite `insufficient_funds_error` (402) being a declared `ErrorType` since F8.3 — now mapped in `ManagedMessagesService.send` via the new `insufficientFunds()` helper. Sender registration cannot block a sandbox managed send by design (`virtual: true` short-circuits it); quiet hours stays proven in `consent/consent.window.spec` because `promoWindowOpen` reads the wall clock and preview accepts no injected `now`, making an HTTP-level test time-of-day dependent. |
| SDK-005-AC06 (post-acceptance blocks → safe refund + terminal event) | `sms-dispatch-recheck.ts` + `managed-recheck.integration`: the queued worker rechecks kill-switch/consent/(live) sender before provider contact; a kill-switch flip or post-acceptance STOP fails message/delivery/attempt with the recorded reason, refunds exactly once, emits the terminal outbox event, and job replay is a no-op. Fail-open on control-plane store errors (posture documented in code); quiet-hours deliberately not re-evaluated. |
| SDK-005-AC07 (deterministic sandbox outcomes agree across wallet/dashboard/retrieval/webhooks) | **Closed** (row previously read "partial" after the work below landed). Inline sandbox resolution propagates delivered status + exact cost onto attempt and delivery (`resource_version` > 1 asserted); the dashboard delivery log/detail/webhooks surfaces shipped 2026-07-17 (item 1 below) and typed webhook consumption is proven end-to-end by `managed-sdk-uat.integration` 2026-07-18 (item 4 below) — the real SDK over real HTTP consuming signed `message.accepted` + `message.delivered` and refusing a tampered payload. |
| SDK-005-AC08 (direct `sms.send` unaffected) | Full API integration suite green (158 tests incl. direct SMS + batch suites) with the managed path in place. |

## Verification executed (2026-07-17)

```text
infisical run --env=dev -- pnpm --filter @app/db migrate
  0080 (hand-reordered: unique index before the attempts containment FK — drizzle-kit ordering
  quirk, same as 0075), 0081 CHECKs, 0082 FORCE RLS + grants applied; policies verified live.

infisical run --env=dev -- npx vitest run --config vitest.integration.config.ts \
  src/messages/managed-messages.integration.spec.ts
  7 passed — idempotent replay, 3-way concurrency race, fingerprint 409, header required,
  max_cost fail-closed, path-coded validation, retrieve with attempts.

infisical run --env=dev -- npx vitest run --config vitest.integration.config.ts   # services/api
  35 files / 158 tests passed (previous statement/batch failures were stale-run DB residue,
  cleaned; documented in HANDOFF).

pnpm --filter @app/wallet --filter @app/sms-engine run test:integration   # 7 + 12 passed
pnpm --filter @fabric-messaging/sdk release:check
  OpenAPI artifact current; 39 SDK tests passed; build + pack + installed-ESM smoke passed.
```

## Verification executed (2026-07-19 amendment)

```text
vitest run --config vitest.integration.config.ts managed-negative
  3 passed — suppressed recipient (400 recipient_opted_out, nothing written), underfunded wallet
  (402 insufficient_funds, nothing written), solvent control send (202, one delivery + one message).
  First run FAILED on the balance case with a 500, which is the defect this suite was written to
  find; re-run green after mapping InsufficientFundsError.

vitest run --config vitest.integration.config.ts        # services/api, full tier
  41 files / 171 tests passed (only senders.integration errors out, and only because the local
  .env lacks DATABASE_URL_PROVISIONER — an env gap, not a code failure).

pnpm --filter @app/api test        30 files / 156 tests passed
pnpm --filter @app/api typecheck   exit 0
```

## Engineering notes

- `expiresAt` must bind as `toISOString()::timestamptz` — the tenant-tx serializer rejects `Date`
  instances (was a 500 on every managed send).
- Retention is SHIPPED end-to-end: `expires_at` + `legal_hold` + partial index
  `idx_message_deliveries_retention`, purged by a daily maintenance cron
  (`maintenance-retention.ts`, advisory-locked, provisioner connection) — capability AND trigger,
  per the "ship the trigger" rule.
- SDK parser (`message-delivery.ts`) is proven against the canonical `@app/contracts`
  `sendManagedMessageResponse` schema (round-trip in `messages.spec.ts`).

## Open before SDK-005 can be called DONE

1. ~~Dashboard delivery logs/details~~ — DONE 2026-07-17 in full: LIST (summaries, recipient
   excluded) + DETAIL (facts, masked recipient, cost, metadata, attempt timeline) + WEBHOOKS
   (`GET /v1/message-deliveries/:id/webhooks` — fan-out state per event/endpoint, secret never
   exposed, containment 404; spec covers empty/populated/404).
2. ~~Managed-path crash-injection~~ — DONE 2026-07-17 (`managed-crash-recovery.integration`).
   ~~Post-acceptance recheck~~ — DONE 2026-07-17: attempt-time rechecks shipped on the queued
   dispatch path + proven (`managed-recheck.integration`).
3. ~~Retention purge job with a production trigger~~ — DONE 2026-07-17.
   ~~Tenant export/deletion handling~~ — DONE 2026-07-18 (`managed-erasure.integration`): the DSR
   subject summary counts the subject's managed deliveries; crypto-shred erasure scrubs their
   caller-supplied reference/metadata in the same transaction as the key destruction (status,
   cost, attempts, ledger survive — a DSR never edits financial history), scrub count audited;
   tenant offboarding = soft-close + the status-blind retention purge (proven on a closed
   tenant), ledger rows surviving by design.
4. ~~Typed webhook consumption + sandbox UAT~~ — DONE 2026-07-18
   (`managed-sdk-uat.integration`): the real SDK over real HTTP sends by key, the standing
   delivery worker fans out, the SDK verifies signatures and consumes typed `message.accepted` +
   `message.delivered`, a tampered payload is refused, and delivery/attempt/wallet/webhook records
   reconcile to one logical message. (The example runs the workspace SDK build; the PACKED-tarball
   variant of the same flow remains a release-gate nicety — `release:check` already packs + smokes.)
5. ~~Managed-specific negative tests for consent/quiet-hours/kill-switch/insufficient-balance~~ —
   DONE 2026-07-19 (`managed-negative.integration`), with the scope calls recorded in AC05 above:
   consent and balance are asserted through HTTP; quiet hours stays at the pure-function tier
   (clock-dependent); sandbox sender status cannot block by design. **Kill-switch remains
   post-acceptance only** — the pre-accept assessment (`assessSendCompliance`) does not consult it,
   so the enforcement point is the attempt-time recheck already proven by `managed-recheck.integration`
   (AC06). If a pre-acceptance kill-switch gate is wanted, that is a behaviour change to specify,
   not a missing test.

**All eight SDK-005 acceptance criteria now trace to passing evidence.** Remaining before the item
can be marked *shipped* rather than *implementation complete*: ADR-0005 sign-off, and the
completion audit's requirement that artifacts exist in the target environment (this work is local
and unpushed).
