# SDK-007 — Send managed Email in sandbox — evidence

**Status:** slices 1 → 4e shipped on `feature/ops-sdk007-email-sandbox` (local commits, nothing
pushed, no package published, no live path enabled). **AC01/AC02/AC03/AC05 closed; AC04 (the inherited
SDK-004-AC02 channel narrowing) is the remaining completion condition — slice 5.**

This document consolidates the per-slice notes (the earlier `sdk-007-slice-4a-ii.md`,
`sdk-007-slice-4c-api.md`, and `sdk-007-slice-4d.md` fragments are superseded by this file).

## What shipped

Managed Email reuses the existing managed-message engine and the existing direct-email
`FakeEmailProvider` sandbox path — no second execution engine. Email is an **extension** of the
channel-aware model reserved in ADR-0005 (Amendment A1, ratified 2026-07-21), not a rebuild: the
`message_deliveries`/`_attempts` tables were already channel-columned; slice 1 relaxed the two
`channel='sms'` CHECKs to `IN('sms','email')`. Live Email stays absent (SDK-009), gated by the
sandbox-environment assertion and the live redline.

## Slice ledger

| Slice | Commit(s) | What |
|-------|-----------|------|
| 1 | `2bb6090`, `63113e9` | Channel-aware contracts + schema. `messageChannel`, `emailVariantContent`, `channel` column on versions (default `sms`, backfilled) + CHECK; delivery/attempt CHECKs → `IN('sms','email')`; `analyzeDefinitionCompatibility` gains `channel_removed`. Version RESPONSE DTO stays SMS-shaped (zero ripple). Migration `0083`. |
| 2 | `2d8a738` | Pure `@app/domain` Email render + preview core. `rateEmailBySize` (size-tiered 50/150/256 KiB, `EMAIL_MAX_BYTES` ceiling), `previewEmail` (subject/text/html render, **HTML-escapes variable values in the html context, rejects CR/LF/U+2028/U+2029 in the subject = header-injection guard**, byte-size measure, tier pricing, path-coded blockers that never echo the value), `previewMessage` channel dispatcher. |
| 3 | `da62641` | Email branch of `POST /v1/messages/preview`. `previewMessageResponse` gains `channel` + nullable `email_preview` (SMS `preview` unchanged). Read-only; email sender `not_evaluated` (sending-domain binding deferred). |
| 4a-i | `fd2a764` | Channel-neutral managed acceptance core + per-channel attempt FK (`email_message_id` + channel-match CHECK). Migration `0084`. SMS money path provably unchanged (18 managed-SMS integration tests). |
| 4a-ii | `739b61e` | Email accept/persist. One tenant tx inserts `email_messages`, reserves the tier price (referenceId = deliveryId), records the dispatch intent, calls the shared acceptance core (`message.accepted` outbox). Migration `0085`. `to = z.union([e164, emailAddress])`; `from` authored on the definition with a synthetic sandbox fallback. |
| 4b-i | `73cbf30` | Managed Email dispatch money resolution — delivered ⇒ commit / undelivered\|failed\|expired ⇒ refund, idempotent, reusing `resolve()`'s `message.updated`. |
| 4b-ii | `ce12fbb` | Attempt-time recheck (`platform.email_sending` kill-switch; email has no consent/opt-out) + TTL crash-recovery sweep; both refund through the 4b-i reconcile; fail-open on store error. Wired into `maintenance runSweep`. |
| 4c | `3d3927c` | Email authoring through the message-definitions API. Create/add-version → `z.discriminatedUnion("channel")` (Email arm has no `sender_id`); channel immutable across versions; publish requires a sender binding only for SMS. Dashboard SMS-narrowed (rich email UI deferred to 4e). |
| 4d | `7f44207` | Test-only hardening: preview↔send byte-parity, send-boundary negatives with before/after side-effect snapshots, email concurrent same-key race, recheck fail-open unit. |
| 4e-i | `f285786` | SDK managed Email support: `MessagePreview.channel` + `emailPreview`; delivery/attempt channel `sms\|email`; `send()` accepts email OR E.164 recipient. |
| 4e-ii | `a8a4eb5` | Dashboard Email authoring UI: channel selector (create-only, immutable), email content fields (from/subject/text/html + per-locale overrides), channel-aware preview panel via the pure `previewEmail`, Edit enabled for email. |

## Acceptance-criteria traceability

| AC | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| AC01 | Released Email preview yields subject/text/HTML, locale, binding status, blockers, price — no send/reservation | **CLOSED** | slice 2 (`email-render.ts`, pure) + slice 3 (`/v1/messages/preview` email branch, read-only, side-effect-count integration) + 4e dashboard preview panel. |
| AC02 | Unchanged `messages.send` with `channel:"email"` completes one funded delivery/attempt through the sandbox provider, observable | **CLOSED** | 4a-ii (accept + reserve) + 4b-i/4b-ii (dispatch/settle/refund/recovery) + 4d parity; `managed-email-dispatch`/`-parity`/`-recovery` real-Postgres specs; 4e-i SDK send accepts an email recipient. |
| AC03 | Unsafe HTML vars / missing content / invalid recipient / oversized / unavailable binding → contextual validation blocks without leaking rejected values | **CLOSED** | slice 2 (html-escape, subject header-injection, size ceiling, path-coded blockers) + 4d `managed-email-negatives` (before/after no-side-effect snapshots, no-PII). |
| AC04 | Generated contracts permit Email only when the released definition includes it (**inherited SDK-004-AC02 channel narrowing** + `@ts-expect-error` fixture) | **OPEN → slice 5** | The catalog generator must emit `channel` per definition and `Fabric<Catalog>` must narrow `messages.send`; a compile fixture proves a channel-unsupported call fails. Not yet implemented. |
| AC05 | A sandbox key can select no live Email provider or sending domain | **CLOSED** | `assertEmailSandboxEnvironment` + `FakeEmailProvider` only; live path absent (SDK-009). |

## Cleanup debt (tracked 2026-07-22, all cleared before slice 5)

1. **`sms_reserve` ledger reason → channel-neutral `message_reserve`** — DONE (`dfb5682`). Additive enum
   migration `0086`; `reservedFor()` matches both new and legacy; backward-compat proven on real
   Postgres (a forged legacy `sms_reserve` reservation still commits + refunds).
2. **`email.service.ts` re-org** — DONE (`6048cbe`). Extracted the `/v1/email` reads to `email-reads.ts`;
   the service is a thinner orchestrator (284 lines).
3. **Unlogged `enqueue().catch` in direct email `send()`** — DONE (`6048cbe`). Now logs the deferral
   (SMS logs the equivalent); send stays fail-open, sweep recovers.
4. **Evidence consolidation** — DONE (this file).
5. **Coverage gaps** — DONE in 4d (`7f44207`) — email concurrent same-key race + recheck fail-open.
6. **Lost `acceptManaged` doc-comment** — DONE (`6048cbe`).

## Verification (real Postgres, local)

- Email + managed money tiers green: `managed-email` (accept/dispatch/parity/negatives/recovery),
  `managed-messages` (SMS), `managed-crash-recovery`, `managed-recheck`, `queue-send`, `idempotency`,
  `maintenance` (sweep), `statement`, `email.integration`, and the new `legacy-reserve-compat` — all
  passing.
- `@fabric-messaging/sdk` `release:check` (typecheck + tests + build + packed ESM smoke) green.
- Dashboard: 14 files / 71 tests (incl. `email-authoring` unit + email route tests); typecheck; biome;
  file-length + browser-safe guards.
- `db:assert:drift` clean (journal reproduces the schema).
- Independent review (codex, read-only) on 4e-i, 4e-ii, and the money migration: nothing blocking
  (4e-ii found + fixed one Medium — the email preview recipient field vs the E.164-only preview
  contract; resolved by removing the field, since email eligibility is not recipient-keyed).

### Known local-env notes (not repo defects)

- `drizzle-kit migrate` fails on this machine: its `__drizzle_migrations` records 82 of 86 journal
  entries (a pre-existing desync that re-applies already-present `0082`–`0085`). The `0086` SQL was
  applied + verified directly via psql (bare and inside a transaction); CI applies the full journal on
  a clean DB (`0086` mirrors the shipped `0061` enum-add pattern).
- `delivery-retention.integration.spec.ts` fails locally: its fixture inserts a `message_delivery_attempts`
  row with no channel-matching message ref, violating the `0084` CHECK — stale-fixture debt predating
  the channel constraint, unrelated to any SDK-007 code.

## Remaining (outside this evidence)

- **Slice 5** — closes AC04: catalog generator emits `channel`; `Fabric<Catalog>` narrows
  `messages.send`; `@ts-expect-error` compile fixture; update `sdk-004.md` AC02 row to "implemented".
- **npm publish** of the SDK (`@fabric-messaging/sdk`) — a separate redline (human go).
- **Live Email** (SDK-009: sending-domain binding + DNS verification + real MTA) — stays behind the
  live redline.
