# SDK-004 generate and verify typed definition catalogs — evidence

> Status: implementation complete; **all acceptance criteria now dispositioned** — updated
> 2026-07-21. Code shipped in `dbfd7d1` (PR #144) alongside SDK-003 slice 6b; this document is the
> retrospective completion audit that should have accompanied it. AC02's channel clause is closed as
> a reviewed non-applicability (product owner, 2026-07-21) with the constraint inherited by
> SDK-007/008 — see "AC02 channel clause". Remaining before the item is *shipped* rather than
> *implementation complete*: the ADR-0005 scope security review, and artifacts existing in the target
> environment (this work is local and unpushed). npm publication stays a separate redline.

## Outcome

`@fabric-messaging/cli` (binary `fabric`) generates a deterministic, type-safe managed-message
catalog for exactly one application environment from a least-privilege `definitions:read` key, and
verifies it against live contracts in CI. Generated output contains no key, message content,
sender/provider identifier, or recipient data. The typed `Fabric<Catalog>` client turns a released
definition's stable key and variable schema into compile-time errors at the call site, while the
same invalid payloads are rejected at runtime by the server-side validator that preview and send
already share.

## Acceptance traceability

| Criterion | Evidence |
| --- | --- |
| SDK-004-AC01 (generate twice, no contract change → byte-identical) | `packages/cli/src/generate.spec.ts` "emits deterministic exact TypeScript without operational identifiers": `generateCatalog(manifest())` run twice asserts `first === second`, and asserts the output contains none of the application/environment UUIDs, `sender`, `provider`, or `body`. |
| SDK-004-AC02 (compile-time key, required data, inline extra fields, channel, locale) | Four of five clauses proven in `packages/sdk/src/catalog.type-test.ts` with `@ts-expect-error` fixtures: unknown stable key, omitted required variable, inline extra variable, out-of-release locale. The fifth (channel) is a **reviewed non-applicability** — see "AC02 channel clause" below: SMS is the only selectable managed channel, so the constraint guards nothing until SDK-007/008, which inherit the clause. |
| SDK-004-AC03 (data widened through JavaScript → same fields rejected at runtime) | The generated types narrow only the call site; the server re-validates every payload through the one `@app/domain` `validatePayload` used by preview and send. Covered by SDK-003 evidence: `message-render` unit tests (path-coded errors, value never echoed) and `message-preview.integration` (invalid payload ⇒ bounded field-path errors, no side effects). |
| SDK-004-AC04 (newer unsupported manifest → upgrade guidance, never emits `any`) | `generate.spec.ts` proves both directions: `parseManifest({manifest_version: 2})` throws `/newer @fabric-messaging\/cli/`, and a manifest demanding `minimum_sdk_contract_version: 2` throws `/Upgrade @fabric-messaging\/sdk/`. Generation is refused before any file is written, so no `any`-typed catalog can be emitted. |
| SDK-004-AC05 (send-only key denied the catalog; definitions-read key cannot send/publish/retrieve content) | Both directions now proven. Forward: `definition-catalog.controller.spec.ts` — a `sms:send` key gets `insufficient_scope`, and an unscoped dashboard token (`*`, null app/env) gets `scoped_api_key_required`. Reverse: `api-key.guard.spec.ts` "denies a definitions:read key every send, publish, and content scope" iterates the closed `apiKeyScopeValues` catalog and asserts `insufficient_scope` for every scope except `definitions:read` — a newly added scope fails this test until deliberately considered. |
| SDK-004-AC06 (sandbox/live mismatch → identifies environment, exits non-zero, no secret material) | `packages/cli/src/commands.spec.ts`: a live catalog fetched by a sandbox key throws `/environment mismatch/`; missing generated output throws `/drift detected for sandbox/` and the thrown message is asserted not to contain the key. `packages/cli/src/bin.ts` additionally redacts `sk_(test|live)_…` from stderr on every failure path and sets `process.exitCode = 1`. |

## Verification executed

```text
pnpm --filter @fabric-messaging/cli release:check                        (exit 0)
  tsc -p tsconfig.build.json
  vitest run                       2 files / 6 tests passed
  node scripts/packed-smoke.mjs    packs the CLI, installs the tarball into a clean ESM consumer,
                                   runs dist/bin.js against a local manifest server, and asserts the
                                   generated file declares `export interface FabricCatalog`
  pnpm pack --dry-run              tarball = dist + README + LICENSE + package.json only

pnpm --filter @app/api test         30 files / 156 tests passed
  (includes the new api-key.guard.spec.ts closed-catalog denial case for AC05 reverse)

pnpm --filter @app/dashboard test   13 files / 60 tests passed
  (includes the rewritten definition-developer-setup.spec.ts, 3 tests)

pnpm --filter @app/api typecheck        (exit 0)
pnpm --filter @app/dashboard typecheck  (exit 0)
```

### Dashboard command-accuracy check — defect found and fixed

The required verification includes "dashboard command accuracy check against the packed CLI". The
shipped `definition-developer-setup.spec.ts` compared `DEFINITION_COMMANDS` to string literals
copied from the same module — a tautology that would survive renaming the binary, the package, or
either subcommand. It has been rewritten to read `packages/cli/package.json` and
`packages/cli/src/bin.ts` and assert the displayed commands against the CLI's declared package name,
its `bin` key, and the subcommand pair the entry point actually accepts.

## AC02 channel clause — reviewed non-applicability (2026-07-21)

**Disposition: not applicable at SDK-004; the compile-time channel constraint is deferred to the
release item that first makes a second channel selectable (SDK-007 managed Email / SDK-008 routing).
Reviewer: product owner (dacsolo10@gmail.com), 2026-07-21.**

Facts. `DefinitionContract.channels` is declared in `packages/sdk/src/catalog.ts` but consumed by no
type: `CatalogPreviewOptions` narrows only `data` and `locale`, and neither `PreviewMessageOptions`
nor `SendMessageOptions` exposes a `channel` field. So the AC02 clause "channel … checked at compile
time" is unimplemented, not merely untested.

Why the underlying risk does not exist today. AC02's purpose is to stop a developer issuing a
compile-valid call against a channel a released definition does not support. SMS is the only
executable managed channel: `UngeneratedCatalog` pins `channels: "sms"`, the generator emits
`channels: "sms"` for every definition, and the send/preview APIs have no channel parameter to
misuse. There is no reachable state in which a caller selects a wrong or unsupported channel, so the
constraint would guard against nothing until a second channel ships. Runtime safety is unaffected
regardless — the API re-validates every payload (AC03), so this is purely about compile-time
ergonomics.

Conditions attached to the deferral (make this reviewable rather than a silent waiver):
- SDK-007 and SDK-008 **cannot** close while adding a second channel unless they also land the
  `channel` narrowing in `CatalogPreviewOptions` (or its send equivalent) plus a `@ts-expect-error`
  fixture proving a channel-unsupported call fails to compile. The clause moves to that item's
  traceability; it is not dropped.
- Until then `DefinitionContract.channels` stays in the generated catalog (the data is correct and
  forward-compatible); only the *type-level enforcement* is deferred.

This satisfies the backlog's no-waiver rule: the criterion is neither silently skipped nor closed on
absent evidence — its risk is recorded as non-existent under the current single-channel surface, with
a named reviewer and an explicit re-activation trigger.

## What is intentionally not in this increment

- Live-environment catalogs: `definitions check` distinguishes sandbox from live, but live releases
  are still refused upstream by SDK-003 (publish targets sandbox only).
- Public npm publication of `@fabric-messaging/cli` — see the external gate below.

## External gate

`@fabric-messaging/cli@0.1.0-beta.1` has **not** been published to npm; `release:check` and
`pack --dry-run` prove it is publishable, not published. Publication is a redline requiring explicit
human authorization, and is tracked alongside the pending `@fabric-messaging/sdk@beta.5` publish.

**ADR-0005 was accepted 2026-07-19** (product owner), so the definitions work this item builds on is
no longer gated on it. Its runtime/management **scope security review remains open** — directly
relevant here, since this item introduced the least-privilege `definitions:read` scope whose
boundary is now covered by the closed-catalog denial test.
